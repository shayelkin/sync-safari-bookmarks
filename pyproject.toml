[project]
name = "sync-safari-bookmarks"
version = "0.1.0"
description = "Sync bookmarks between Chrome and Safari via native messaging"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "pytest>=8",
    "hypothesis>=6",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
